{
  "name": "green-it",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tw:watch": "npx tailwindcss -i ./src/style/app.css -c tailwind.config.cjs -o src/style/index.css --watch",
    "tw:build": "NODE_ENV=production npx tailwindcss -i ./src/style/app.css -c tailwind.config.cjs -o ./dist/assets/index.css --minify"
  },
  "dependencies": {
    "preact": "^10.13.0",
    "preact-router": "^4.1.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "tailwindcss": "^3.2.7",
    "vite": "^4.2.0"
  }
}
